import { exec } from 'child_process';
export class VideoUtils {
    // static ipfs:string = '/usr/local/bin/ipfs';
    // static uploadFileToIPFS(filePath:string ) {
    //     const cmd = `${this.ipfs} add -r --quieter ${filePath} 2>&1`;
    //     exec(cmd, (err, stdout, stderr) => {
    //         if (err) {
    //             console.log(stderr);
    //             callback(false);
    //         } else {
    //             this.deleteFile(filePath, (deleted) => {
    //                 callback(deleted);
    //             });
    //         }
    //     });
    // }
    
    // deleteFile(filePath:string, callback) {
    //     fs.unlink(filePath, (err) => {
    //         callback(!err);
    //     });
    // } 
}